galaxy_info:
  author: Paul Lyon
  description: Ansible role to install and configure puppet agent.
  issue_tracker_url: https://github.com/the-paulus/puppet-agent/issues
  license: BSD

  min_ansible_version: 1.2

  github_branch: master

  platforms:
  - name: all
    versions:
      - all

  galaxy_tags:
  - automation
  - puppet
  - configuration

dependencies:
- src: the-paulus.puppet
